# выбрать из датафрейма только те строки, где значение в столбце country равно Italy

import pandas as pd

df = pd.read_excel('file-name.xlsx')  # pd.read_csv('file-name.csv')

italy_wine = df.loc[df.country == 'Italy']

print(italy_wine.head())  # для проверки отработанного выше кода
